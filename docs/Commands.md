## Commands
### chunk remove
`/chunk remove <ChunkPosX> <ChunkPosZ>`

删除存档中的指定区块使得重载时让该区块重新生成，没有后边的参数则删除玩家所在的区块
### rules
`/rules`
* `close`将所有已加载的carpet规则(类型为boolean)设置为false
* `enabled`将所有已加载的carpet规则(类型为boolean)设置为true
* `def`将所有已加载的carpet规则设为默认值